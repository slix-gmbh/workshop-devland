apiVersion: apps/v1
kind: Deployment
metadata:
  name: rideshare
  namespace: rideshare
  labels:
    app.kubernetes.io/component: region1
    app.kubernetes.io/instance: rideshare-demo
    app.kubernetes.io/name: rideshare
    app: rideshare
    profiling: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rideshare
  template:
    metadata:
      annotations:
        prometheus.io.scrape: "true"
      labels:
        app.kubernetes.io/component: region1
        app.kubernetes.io/instance: rideshare-demo
        app.kubernetes.io/name: rideshare
        app: rideshare
        profiling: "true"
    spec:
      securityContext: {}
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      automountServiceAccountToken: true
      containers:
      - name: rideshare
        image: fra.ocir.io/frsjez6mlvwx/slixpublic/ride-share-auto:1.3.1
        imagePullPolicy: Always
        env:
        - name: ENABLE_MEMORY_TEST
          value: "false"
        - name: ENABLE_LOCK_TEST
          value: "false"
        - name: POD_IP
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: status.podIP
        - name: OTEL_RESOURCE_ATTRIBUTES
          value: ip=$(POD_IP)
        ports:
        - name: http
          containerPort: 5000
        resources:
          requests:
            memory: "8Mi"
            cpu: "50m"
          #limits:
          #  memory: "1024Mi"
          #  cpu: "300m"
        volumeMounts:
        - mountPath: /tmp
          name: tmp
      volumes:
      - emptyDir: {}
        name: tmp
